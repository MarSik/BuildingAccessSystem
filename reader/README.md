# remote RFID card reader with RS485 interface

## Building

- Install embedded cmake tooling (github.com/MarSik/embc)
- run `embc init mke04` to initialize the build environment
- run `embc build` to build

